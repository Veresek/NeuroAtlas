services:
  client:
    build:
      context: ./client
    restart: unless-stopped
    ports:
      - "5173:5173"

  server:
    build:
      context: ./server
    env_file:
      - ./.env
    restart: unless-stopped
    ports:
      - "8000:8000"
